import theme from './theme';

const colors = {
  theme,
};

const sizes = {
  none: '0px',
  sm: '4px',
  md: '8px',
  lg: '16px',
};

const mainTheme = {
  colors,
  sizes,
};

export default mainTheme;
